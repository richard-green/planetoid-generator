import type { PlanetoidPresetSettings } from '../../lib/components/Threlte/Planetoid/PlanetoidSettings'

export type PlanetoidPreset = {
  id: string
  name: string
  settings: PlanetoidPresetSettings
}
